<div class="row no-margin" style="margin-bottom: 80px !important;">
    <nav class="navbar navbar-expand-lg navbar-light bg-light navbar_content nav_bar_div">
        <a class="navbar-brand text_link" href="{{ path('index') }}">Young developers</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
                <li class="nav-item active">
                    <a class="nav-link text_link" href="{{ path('bio') }}">Acerca de<span class="sr-only">(current)</span></a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text_link" href="{{ path('contact') }}">Contacto</a>
                </li>
            </ul>
            <ul class="navbar-nav my-2 my-lg-0">
                <li class="nav-item">
                    <a class="nav-link text_link" href="{{ path('login') }}">Login</a>
                </li>
            </ul>
        </div>
    </nav>
</div>